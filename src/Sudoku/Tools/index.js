import { ToolsBox, ToolsButton } from "./styled";

const Tools = () => {
    return (
        <ToolsBox>
            <ToolsButton>Clear Sudoku</ToolsButton>
        </ToolsBox>
    );
}

export default Tools;